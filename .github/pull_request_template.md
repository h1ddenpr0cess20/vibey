# Summary
What changed and why. For bug fixes, note the root cause briefly.

## Testing
How you validated it — `npm test`, `npm run lint`, and a real run (`npm run dev`,
or `npm run dev:lan` if it touches the mic, audio, or anything that needs a
secure context). Say plainly what you could not test.

## Notes (optional)
Known limitations or follow-ups.
